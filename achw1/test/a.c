#include <stdio.h>
#include <stdlib.h>
int sum(int c,int d){
	return c+d;
}
int main(){
	int a , b , c , d,e;
	a = 5;
	b = 6;
	c = a + b;
	d = a + b;
	//return (c == d ? 1 : 0);
	if( c != d ){
		int g[c];
		puts("Hello World");
		c == d ? sum(a,b) : 0;
		malloc(30);
		return 1;
		malloc(20);
	}
	if(5==5 ){
		puts("WTF Wolrd");
		malloc(10);
	}
	sum(a,b);
	
	return 0;
}
